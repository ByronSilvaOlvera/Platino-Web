import { createSelector, createFeatureSelector } from "@ngrx/store";
import { AppStateC } from "./app.state";

// export const completaPg = createSelector(
//     (state:AppStateC) => state.completa
// )